{
  "hooks": {
    "event": "PreToolUse",
    "tools": ["edit_file", "replace_string_in_file", "create_file"],
    "command": "branch=$(git -C /Users/lpineda/Desktop/InOfficeDaysTracker rev-parse --abbrev-ref HEAD 2>/dev/null); if [ \"$branch\" = \"main\" ]; then echo 'BLOCK: You are on main. Create a feature branch first: git checkout -b feature/<name>'; exit 1; fi"
  }
}
